import React, { Component } from "react";

class Retrieve extends Component {
  state = { images: [] };
  componentDidMount() {
    fetch("");
  }

  render() {
    return (
      <React.Fragment>
        <p>sss</p>
      </React.Fragment>
    );
  }
}

export default Retrieve;
